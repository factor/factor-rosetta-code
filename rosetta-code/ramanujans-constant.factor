! Calculate Ramanujan's constant (as described on the OEIS site) with at
! least 32 digits of precision, by the method of your choice. Optionally,
! if using the 𝑒**(π*√x) approach, show that when evaluated with the last
! four Heegner numbers the result is almost an integer.

