! Task:
! 
! Write a program that can parse a grammar in Extended Backus–Naur Form
! (EBNF), and then parse something else according to the grammar.
! 
! The program is only required to decide whether or not the something else
! belongs to the language described by the grammar, but for extra credit,
! it can output a syntax tree.
! 
! See the tests.
! 
! Category:Recursion


